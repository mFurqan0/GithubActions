name: "Workflow Commands"

on: ["push"]

jobs:
  my-job:
      runs-on: ubuntu-latest
      steps:
          - name: "group logging"
            run: |
              echo "::group:: My group message"
              echo  "Msg 1"
              echo  "Msg 2"
              echo "::endgroup::"
          - name: "step 1"
            run: |
              echo "Kaisa hai bhai"
          - name: "step 2"
            run: |
              echo "kia haal chal hai"